program evenFibonacciSum
    implicit none
    
    integer, parameter :: limit = 4000000



end program evenFibonacciSum

recursive function evenFibSum(limit) result(j)
    integer, intent (in) :: limit
    
    !if fib is less than limit, call recursive again
    !else return 
end function evenFibSum
